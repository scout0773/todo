1. save/export tasks to CSV file - saves and loads txt files, will add CSV support later
2. priority levels - added automatic sorting from top to bottom priority [DONE]
3. multi-delete
4. due dates/reminders
5. make window un-resizable
6. fix problem where editing task of same priority only affects other instead of selected